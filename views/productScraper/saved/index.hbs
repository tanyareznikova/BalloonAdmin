
<a class="newspaper-headline" href="/">
  <h1 class="newspaper-font text-center">Связной +</h1>
</a>
<div class="d-flex intro-bar">
  <div class="p-2 flex-grow-1">
    <h5 class="newspaper-font text-capitalize">{{title}}</h5>
    <p class="card-text intro-text">Сейчас {{qty}} смартфонов в БД.</p>
  </div>

  <div class="p-2">
    <a href="/" class="btn btn-sm btn-outline-dark">
      <i class="fas fa-home"></i> Домой
    </a>
  </div>
</div>
<div class="content-bar">
  {{# each products }}
  <div class="row">
    {{# each this}}
    <div class="col-12 col-md-4">
      <div class="card mx-auto mb-3" style="width: 90%;">
        <img class="card-img-top img-fluid div-205 mx-auto mt-2" src="{{this.imgUrl}}" alt="Card image cap">
        <div class="card-body">
          <a href="{{this.link}}" target="_blank" class="card-title news-title">{{this.title}}</a>
          <p class="card-text mt-2">{{this.price}}</p>
          <a href="/articles/{{this._id}}" class="view-comments btn btn-link"><i class=" far fa-comments"></i> Просмотреть
            Отзывы
          </a>
          <a href="/save-article/{{this._id}}" class="btn btn-sm round btn{{#this.isSaved}}-outline{{/this.isSaved}}-dark">
            {{this.buttonStatus}}
          </a>
        </div>
      </div>
    </div> {{/each}} </div>
  {{/each}} </div> {{!-- End Content Bar --}}

